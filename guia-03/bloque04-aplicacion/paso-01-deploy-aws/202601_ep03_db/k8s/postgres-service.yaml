apiVersion: v1
kind: Service
metadata:
  name: alumnos-db
  namespace: alumnos
spec:
  selector:
    app: alumnos-db
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
