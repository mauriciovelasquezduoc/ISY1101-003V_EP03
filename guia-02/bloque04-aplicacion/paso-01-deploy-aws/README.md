# Deploy de aplicaciones

# Se debe crear un token 

icono izquierdo -> settings -> developers settings -> Personal access Token -> tockens (classic) -> Generate new token (classic)

## DB

```
cd 202601_ep03_db
git init
git add .
git commit -m "first commit"
git branch -M main
git remote set-url origin https://github.com/ALUMNO-GIT/202601_ep03_db.git
git push -u origin main

```
